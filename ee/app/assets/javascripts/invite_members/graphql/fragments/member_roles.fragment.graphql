fragment MemberRoles on MemberRoleConnection {
  nodes {
    baseAccessLevel {
      integerValue
    }
    id
    name
    description
    membersCount
    editPath
    enabledPermissions {
      nodes {
        name
        value
      }
    }
  }
}
