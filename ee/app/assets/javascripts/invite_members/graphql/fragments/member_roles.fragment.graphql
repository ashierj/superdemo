fragment MemberRoles on MemberRoleConnection {
  nodes {
    baseAccessLevel {
      integerValue
    }
    id
    name
    description
    membersCount
    enabledPermissions {
      nodes {
        name
        value
      }
    }
  }
}
