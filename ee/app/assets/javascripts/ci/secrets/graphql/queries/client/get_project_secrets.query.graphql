query getProjectSecrets($fullPath: ID!, $offset: Integer, $limit: Integer) {
  project(fullPath: $fullPath) {
    id
    fullPath
    secrets(offset: $offset, limit: $limit) @client {
      count
      nodes {
        key
        name
        labels
        lastAccessed
        createdAt
      }
    }
  }
}
