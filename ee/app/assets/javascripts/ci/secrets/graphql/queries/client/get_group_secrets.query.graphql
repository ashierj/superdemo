query getGroupSecrets($fullPath: ID!, $offset: Integer, $limit: Integer) {
  group(fullPath: $fullPath) {
    id
    fullPath
    secrets(offset: $offset, limit: $limit) @client {
      count
      nodes {
        key
        name
        labels
        lastAccessed
        createdAt
      }
    }
  }
}
